/** Address for Order (snapshot of the customer address details when order created) */

import { type OrderAddress } from '@/graphql/schema/graphql'

export { type OrderAddress } from '../schema/graphql'
export type OrderAddresses = OrderAddress[]